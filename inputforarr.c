#include <stdio.h>
int main(){
    int arr[5];

    for(int i=0;i<=4;i++){
        printf("enter the element for %d row and %d column:",i,j);
        scanf("%d",&arr[i]);
        
    }
    printf("%d",arr[2]);
    return 0;
}